{
  "name": "connector-nextjs",
  "version": "1.12.0",
  "description": "NextJs specific connector.",
  "main": "dist/index.js",
  "scripts": {
    "versionbump": "npm version minor",
    "build": "tsc && ts-node bundler/bundle.ts",
    "rebuild": "rm -rf ./dist/ && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "connector"
  ],
  "author": "Adam Jakab <adam@jakab.pro>",
  "license": "MIT",
  "dependencies": {
    "@queue-it/connector-javascript": "^4.3.0",
    "@queue-it/queue-token": "^1.0.4",
    "jssha": "^3.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.12.12",
    "esbuild": "^0.21.3",
    "esbuild-plugin-tsc": "^0.4.0",
    "next": "^13.5.6",
    "ts-node": "^10.9.2"
  }
}
